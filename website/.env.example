DATABASE_URL=

# Public URL of this frontend (no trailing slash)
# Used as the redirect base for OAuth callbacks and Stripe return URLs
NEXT_PUBLIC_APP_URL=https://eisenlabs.com

# Backend auth service base URL (no trailing slash)
# Defaults to the Cloud Run URL; override once auth.eisenlabs.com DNS is propagated
NEXT_PUBLIC_AUTH_URL=https://eisen-auth-923944622611.europe-west1.run.app
